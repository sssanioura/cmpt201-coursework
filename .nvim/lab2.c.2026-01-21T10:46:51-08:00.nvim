#include <stdio.h>

void main() {
  printf("Enter programs to run. \n");
  // recieve user input
  ssize_t 

user_input = getLine();
  execl();

  /// print on screen
  // tokenize
  //
  // execl program
  // fork a new process for execl
  // wait using waitpid
  // run file / open file
}
